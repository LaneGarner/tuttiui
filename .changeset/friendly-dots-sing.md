---
"tuttiui": minor
---

Replace the package-name placeholder with a single-install entry point that re-exports the React component library and design tokens. Add explicit `react`, `tokens`, and `tailwind` subpaths plus the generated theme stylesheet.
